# API base URLs are currently defined in src/lib/axios.ts
# No API keys are required.

# Access-token lifetime in minutes (default: 30)
# Set to 1 to test the token refresh flow.
# VITE_ACCESS_TOKEN_TTL_MINS=1
