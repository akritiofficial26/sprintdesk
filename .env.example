# Base URLs are hardcoded in src/lib/axios.ts for this assignment since both
# APIs are public and require no keys. If you productionize this, move them
# here as VITE_DUMMYJSON_BASE_URL / VITE_JSONPLACEHOLDER_BASE_URL.
